import type { NextPage } from 'next'
import Head from 'next/head'
import Image from 'next/image'
import styles from '../styles/Home.module.css'

const Home: NextPage = () => {
  return (
    <div>
      <Head>
        <title>Vigetlabs Organization Members Audit</title>
        <meta name="description" content="Keep track of members and collaborators" />
        <link rel="icon" href="/favicon.ico" />
      </Head>

      <main>
        home
      </main>
    </div>
  )
}

export default Home
